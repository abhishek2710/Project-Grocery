

const idInput=document.querySelector('#input-id');
const nameInput=document.querySelectqor('#input-name');
const addressInput=document.querySelectqor('#input-address');
const mailInput=document.querySelectqor('#input-mail');
const numInput=document.querySelectqor('#input-num');
const confirmBtn=document.querySelector('#btn-confirm');

const clear=()=>{
    idInput.value='';
    nameInput.value='';
    addressInput.value='';
    mailInput.value='';
    numInput.value='';

};

confirmBtn.addEventListener('click',()=>{
    const enteredId=idInput.value;
    const enteredName=nameInput.value();
        const enteredAddress=addressInput.value();
    const enteredMail=mailInput.value();
    const enteredNum=numInput.value();

   
    
});
cancelBtn.addEventListener('click',clear);
